import sys

# simple argument echo script
#for v in sys.argv[1:]:
#  print v



# USAGE
# python detect_blur.py --images images

# import the necessary packages
from imutils import paths
import argparse
import cv2



#imagePath=sys.argv[1]#args["images"]

#import sys
#exit_code = sys.argv[1]
#sys.exit(exit_code)


#fm = cv2.Laplacian(cv2.cvtColor(cv2.imread(imagePath), cv2.COLOR_BGR2GRAY), cv2.CV_64F).var()
#text = "Not Blurry"
'''
# if the focus measure is less than the supplied threshold,
# then the image should be considered "blurry"
'''
#if fm < 90:
#	text = "Blurry"
'''
# show the image
#cv2.putText(image, "{}: {:.2f}".format(text, fm), (10, 30),
#	cv2.FONT_HERSHEY_SIMPLEX, 0.8, (0, 0, 255), 3)
#cv2.imshow("Image", image)
savedpath='image ro inja zakhire kardam'
#key = cv2.waitKey(0)

print savedpath


'''
#print imagePath

sys.exit(1)
